#include <iostream>
using namespace std;

int main(){
    int x;
    long long y;
    cin >> x >> y;
    cout << x <<" + " << y << " = " << (x+y) << endl ;
    cout << x <<" * " << y << " = " << (x*y) << endl ;
    cout << x <<" - " << y << " = " << (x-y);
    return 0;
}